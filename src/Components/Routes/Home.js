import React from 'react';
import Slider from '../Carousel/Slider';

import styled from 'styled-components';

const Home = () => {
  return (
    <>
      <Slider />
    </>
  );
};

export default Home;
